[metadata]
name = compiler_sheesh

[options]
package_dir=
    =source
packages = find:
zip_safe = False
python_requires = >= 3
[options.packages.find]
where = source
exclude =
    tests*
    .gitignore